
  # Create Styleguide Page

  This is a code bundle for Create Styleguide Page. The original project is available at https://www.figma.com/design/jz6LwBE4CllSRSPNoI4lcb/Create-Styleguide-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  